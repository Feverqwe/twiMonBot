{
  "version": "2",
  "services": {
    "bot": {
      "build": ".",
      "ports": [
        "1339:1339"
      ],
      "volumes": [
          "./log:/opt/backend/log"
      ],
      "restart": "always",
      "network_mode": "host"
    }
  }
}
# Shared source

Every file in this directory is shared infrastructure and must remain byte-for-byte identical in
`twiMonBot` and `ytWatchBot`.

Run `npm run shared:check` after changing a shared file. Make the same change in both repositories
in one work session.


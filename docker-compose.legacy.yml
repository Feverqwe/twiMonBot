{
  "version": "2",
  "services": {
    "bot": {
      "env_file": [
        ".env"
      ],
      "build": ".",
      "volumes": [
        "/var/log/twiMonBot:/opt/log"
      ],
      "restart": "always",
      "network_mode": "host"
    }
  }
}

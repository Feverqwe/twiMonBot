{
  "services": {
    "bot": {
      "env_file": [
        ".env"
      ],
      "build": ".",
      "volumes": [
        "./log:/opt/log",
        "./store:/opt/store"
      ],
      "restart": "always",
      "network_mode": "host"
    }
  }
}
